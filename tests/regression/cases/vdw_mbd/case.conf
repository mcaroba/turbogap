# Many-body dispersion. This is the single largest master-only block in
# turbogap.f90 and the first phase to be extracted, so it is covered at both
# 1 and 2 ranks.
DATA=vdw_P
MODE=predict
RANKS=1
LINKS="p4_dimer.xyz gap_files"
NEEDS="p4_dimer.xyz gap_files/phosphorus.gap"
OUTPUTS="trajectory_out.xyz"
