# 7176-atom single point. This is the timing reference: the same case the GPU
# benchmarks in docs/gpu_fixes_handoff.md use, so numbers stay comparable.
DATA=CO
MODE=predict
RANKS=1
LINKS="atoms_7176.xyz:atoms.xyz gap_files"
NEEDS="atoms_7176.xyz gap_files/CO.gap"
OUTPUTS="trajectory_out.xyz"
