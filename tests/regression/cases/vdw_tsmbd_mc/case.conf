# ts+mbd in mc mode, which like predict never advances md_istep past -1 and so
# also segfaulted before the fix. 5 'move' steps at a fixed seed.
# REFERENCE=golden for the same reason as vdw_tsmbd_predict; see that case.
DATA=vdw_P
MODE=mc
RANKS=1
REFERENCE=golden
LINKS="p4_cell.xyz gap_files"
NEEDS="p4_cell.xyz gap_files/phosphorus.gap"
OUTPUTS="mc.log mc_all.xyz"
