# TurboGAP keywords

Generated by `tools/keyword_docs.py` from `src/read_files.f90` and
`src/types.f90`. Do not edit this file: edit the `!>` block above the
keyword in `src/read_files.f90` and regenerate with `make docs`.

## Contents

**The input file** &mdash; 239 keywords

- [General](#general) (12)
- [Run control](#run-control) (17)
- [Molecular dynamics](#molecular-dynamics) (17)
- [Nested sampling](#nested-sampling) (6)
- [Monte Carlo](#monte-carlo) (31)
- [Van der Waals](#van-der-waals) (31)
- [Electrostatics](#electrostatics) (11)
- [Experimental data (MAD)](#experimental-data-mad) (53)
- [Electronic stopping and EPH](#electronic-stopping-and-eph) (32)
- [Local properties](#local-properties) (5)
- [Output](#output) (19)
- [GPU](#gpu) (5)

**The potential (.gap) file** &mdash; 59 keywords

- [soap_turbo](#soap-turbo) (40)
- [distance_2b](#distance-2b) (7)
- [angle_3b](#angle-3b) (9)
- [core_pot](#core-pot) (3)

# Keywords for the input file

One `keyword = value` per line. A keyword this list does not contain
aborts the run.

## General

The atoms, the species and how chatty the run is.

| Keyword | Type | Default | Units | Modes | Description | Depends on |
| --- | --- | --- | --- | --- | --- | --- |
| `atoms_file`<br>`input_file` | string |  |  | all | Path to the extended-XYZ file holding the atomic configuration, or the trajectory of configurations to loop over in predict mode. Required. |  |
| `e0` | real list |  | eV | all | Constant energy offset per species, added to every atom of that species. One value per entry in species, in the same order. The GAP predicts energies relative to these, so they set the zero of the energy scale. |  |
| `masses` | real list |  | amu | all | Atomic mass of each species, one value per entry in species and in the same order. Read in amu and converted internally to eV fs^2 / A^2. If absent they are taken from the XYZ file instead. | see `species` |
| `max_gbytes_per_process` | real | `1.0` | GB | all | Memory an MPI rank may use for the SOAP descriptor and its derivatives; the rank splits its atoms into as many batches as it takes to stay under this. Naming it in the input file also stops the run sizing the budget from the node automatically, so a value chosen deliberately is never overwritten. | sets `max_Gbytes_set`; see `mem_fraction` |
| `mem_fraction` | real | `0.25` |  | all | Fraction of the node's memory to divide between the ranks on it when max_Gbytes_per_process was not given. Only consulted for that automatic budget, and ignored once max_Gbytes_per_process appears in the deck. | see `max_gbytes_per_process` |
| `neighbors_buffer` | real | `0.0` | A | all | Extra distance added to every cutoff when the neighbour lists are built, so that a list stays valid for several steps as atoms move. Larger values cost memory and neighbour-loop time but rebuild less often. |  |
| `radii` | real list |  | A | all | Per-species radius used by the Monte-Carlo insertion and accessible-volume tests, one value per entry in species. Not a physical parameter of the potential. | see `accessible_volume`; see `mc_types` |
| `random_seed` | integer | `0` |  | all | Seed for the intrinsic pseudo-random number generator. Zero, the default, leaves the compiler's own sequence alone; any other value makes a run repeatable, which is what the regression comparisons rely on when the initial velocities are randomized. | see `randomize_velocities` |
| `species` | string list |  |  | all | Chemical symbols of the species in the system, in the order every other per-species list is written in. n_species entries. |  |
| `timing` | logical | `false` |  | all | Print a breakdown of where wall time went, by phase, at the end of the run. |  |
| `verbosity`<br>`verb` | integer | `0` |  | all | How much diagnostic output to print. 0 is quiet; the MC and MD drivers gate extra reporting on thresholds up to about 50. |  |
| `which_atom` | integer | `0` |  | predict | Restrict the prediction to a single atom index, for debugging a local environment. 0, the default, means every atom. |  |

## Run control

What is computed, and how a relaxation is driven.

| Keyword | Type | Default | Units | Modes | Description | Depends on |
| --- | --- | --- | --- | --- | --- | --- |
| `do_derivatives` | logical | `false` |  | all | Compute the derivatives of the SOAP descriptor with respect to atomic positions. Needed for forces, and switched on automatically in md and mc mode. Turning it off in predict mode is what makes an energy-only pass cheap. |  |
| `do_derivatives_fd` | logical | `false` |  | all | Compute the descriptor derivatives by finite differences instead of analytically. A correctness check on the analytic route, far slower, and not meant for production. | see `do_derivatives` |
| `do_forces` | logical | `false` |  | all | Compute forces and the virial. Set automatically in md, mc and predict mode; the only reason to name it is to switch it off. |  |
| `do_mc` | logical | `false` |  | mc | Run a Monte-Carlo walk. Set automatically by `turbogap mc`. |  |
| `do_md` | logical | `false` |  | md | Run molecular dynamics. Set automatically by `turbogap md`. |  |
| `do_prediction` | logical | `false` |  | all | Evaluate the GAP at all. Set automatically in every mode except soap; switching it off leaves only the descriptor calculation. |  |
| `e_tol` | real | `1.e-6` | eV | all | Convergence threshold on the energy change between successive relaxation steps. The relaxation stops when the energy, force and pressure criteria are all met. | needs `optimize`; see `f_tol`; see `p_tol` |
| `f_tol` | real | `0.01` | eV/A | all | Convergence threshold on the largest force component during a relaxation. | needs `optimize`; see `e_tol`; see `p_tol` |
| `gamma0` | real |  |  | all | **Accepted and ignored.** The gradient-descent prefactor this once scaled. The step is now taken from max_opt_step and the largest force, so the value is read and discarded. | see `max_opt_step` |
| `max_opt_step` | real | `0.1` | A | all | Largest distance any atom may move in one gradient-descent step. The step size is chosen so that the biggest displacement equals this. | needs `optimize` |
| `max_opt_step_eps` | real | `0.05` |  | all | Largest strain any cell vector may take in one step of a cell relaxation. The strain analogue of max_opt_step. | needs `optimize`; see `max_opt_step` |
| `optimize` | string | `vv` |  | md, mc | How the geometry is driven: "vv" for velocity-Verlet dynamics, "gd" for gradient descent on the positions, "gd-box" to relax the cell as well, and "gd-box-ortho" to relax the cell keeping it orthorhombic. Anything else aborts the run. |  |
| `p_tol` | real | `0.01` | GPa | all | Convergence threshold on the pressure during a cell relaxation. | needs `optimize`; see `e_tol`; see `f_tol` |
| `target_pos_step` | real |  | A | md | Displacement the variable time step aims for: the step is rescaled so that the fastest atom moves about this far. Naming this keyword is what enables the variable time step. | sets `variable_time_step`; see `tau_dt` |
| `tau_dt` | real | `100.0` | fs | md | Relaxation time over which the variable time step is allowed to change, so that the step size follows target_pos_step smoothly instead of jumping. | needs `target_pos_step` |
| `write_derivatives` | logical | `false` |  | all | Write the SOAP descriptor derivatives to file alongside the descriptors. Large output; a debugging aid. | see `write_soap` |
| `write_soap` | logical | `false` |  | all | Write the SOAP descriptor vectors to file. Set automatically by `turbogap soap`. |  |

## Molecular dynamics

Time stepping, thermostat and barostat.

| Keyword | Type | Default | Units | Modes | Description | Depends on |
| --- | --- | --- | --- | --- | --- | --- |
| `barostat` | string | `none` |  | md, mc | Pressure coupling: "none" or "berendsen". Anything else aborts the run. | see `p_beg`; see `p_end`; see `tau_p`; see `barostat_sym` |
| `barostat_sym` | string | `isotropic` |  | md, mc | Which components of the cell the barostat is allowed to change: "isotropic" scales all three axes together, and the anisotropic settings let them move independently. | needs `barostat` |
| `box_scaling_factor` | real(3, 3) | `identity` |  | all | A 3x3 matrix the cell is multiplied by, applied once at the start of the run. Nine numbers, read column by column. Used to strain a cell without editing the XYZ file. | needs `scale_box` |
| `gamma_p` | real | `1.0` |  | md, mc | Damping of the Berendsen barostat's cell response, on top of tau_p. | needs `barostat` |
| `md_nsteps` | integer | `1` |  | md | Number of molecular-dynamics steps to take. |  |
| `md_step` | real | `1.0` | fs | md | Time step. With a variable time step this is the starting value. | see `target_pos_step` |
| `n_t_hold` | integer | `0` |  | md | Number of entries in the t_hold list, which must be given before it. | sets `t_hold`; see `t_hold` |
| `p_beg` | real | `1.0` | GPa | md, mc | Target pressure at the start of the run. With p_end it defines a linear ramp over the run. | needs `barostat` |
| `p_end` | real | `1.0` | GPa | md, mc | Target pressure at the end of the run. | needs `barostat`; see `p_beg` |
| `randomize_velocities` | logical | `false` |  | md | Draw the initial velocities from a Maxwell-Boltzmann distribution at t_beg instead of taking them from the XYZ file. Use random_seed to make the draw repeatable. | see `random_seed`; see `t_beg` |
| `scale_box` | logical | `false` |  | all | Apply box_scaling_factor to the cell at the start of the run. | see `box_scaling_factor` |
| `t_beg` | real | `300.0` | K | md, mc | Target temperature at the start of the run. With t_end it defines a linear ramp over the run; give only t_beg for a constant-temperature run. | see `thermostat`; see `t_end`; see `t_hold` |
| `t_end` | real | `300.0` | K | md, mc | Target temperature at the end of the run. | see `t_beg` |
| `t_hold` | real list |  | K | md | Segments of the temperature schedule, as a flat list read n_t_hold entries at a time: a piecewise ramp-and-hold instead of the single linear ramp that t_beg and t_end give. n_t_hold must appear first. | needs `n_t_hold`; sets `n_t_hold` |
| `tau_p` | real | `1000.0` | fs | md, mc | Relaxation time of the barostat: how quickly the cell is pushed towards the target pressure. Larger is gentler. | needs `barostat` |
| `tau_t` | real | `100.0` | fs | md, mc | Relaxation time of the thermostat: how quickly the kinetic energy is pushed towards the target temperature. Larger is gentler. | needs `thermostat` |
| `thermostat` | string | `none` |  | md, mc | Temperature coupling: "none", "berendsen" or "bussi". Bussi is the stochastic velocity rescaling that samples the canonical ensemble properly; Berendsen does not. Anything else aborts the run. | see `t_beg`; see `t_end`; see `tau_t` |

## Nested sampling

Nested-sampling walks.

| Keyword | Type | Default | Units | Modes | Description | Depends on |
| --- | --- | --- | --- | --- | --- | --- |
| `n_nested` | integer | `0` |  | all | Number of nested-sampling iterations. Naming it is what enables nested sampling. | sets `do_nested_sampling` |
| `nested_max_strain` | real | `0.0` |  | all | Largest strain a nested-sampling cell-shape move may apply. |  |
| `nested_max_volume_change` | real | `0.0` |  | all | Largest fractional volume change a nested-sampling volume move may make. | needs `scale_box_nested` |
| `p_nested` | real | `0.0` | GPa | all | External pressure entering the nested-sampling enthalpy. | needs `n_nested` |
| `scale_box_nested` | logical | `false` |  | all | Let nested sampling change the cell as well as the positions. | needs `n_nested`; see `nested_max_volume_change`; see `nested_max_strain` |
| `t_extra` | real |  |  | all | **Accepted and ignored.** Temperature offset from an earlier nested-sampling design; nothing consults it. |  |

## Monte Carlo

Move types, acceptance and the grand-canonical ensemble.

| Keyword | Type | Default | Units | Modes | Description | Depends on |
| --- | --- | --- | --- | --- | --- | --- |
| `accessible_volume` | logical | `false` |  | mc | Use the volume actually accessible to a new atom, with the volume of the existing atoms' spheres removed, rather than the cell volume, in the insertion and removal acceptance ratios. The spheres are sized by radii. | see `radii` |
| `mc_acceptance` | real list |  |  | mc | Relative probability of picking each move type, one weight per entry in mc_types and in the same order. Normalised internally, so the weights need not sum to one. Its length sets n_mc_types. | sets `n_mc_types`; see `mc_types` |
| `mc_hamiltonian` | logical | `false` |  | mc | Include the kinetic energy in the Metropolis test, so that the walk samples configurations and momenta together rather than configurations alone. Also what makes the "md" move type meaningful. | see `mc_types` |
| `mc_hybrid_opt` | string | `vv` |  | mc | How the short MD burst of a hybrid "md" move is propagated: same choices as optimize. | needs `mc_types`; see `optimize` |
| `mc_lnvol_max` | real | `0.01` |  | mc | Largest change in ln(V) a volume move may make, so the trial volume is drawn from V*exp(+-mc_lnvol_max). Working in the logarithm keeps the move symmetric in volume. | needs `mc_types` |
| `mc_max_dist` | real | `100000000.0` | A | mc | Largest distance from an existing atom at which a new atom may be inserted. Together with mc_min_dist it bounds where an insertion trial may land. | see `mc_min_dist`; see `mc_max_insertion_trials` |
| `mc_max_dist_to_planes` | real list |  | A | mc | Distance from each plane within which a moved or inserted atom must stay, one value per plane. mc_n_planes must appear first. | needs `mc_n_planes`; sets `mc_n_planes`; see `mc_planes` |
| `mc_max_insertion_trials` | integer | `500` |  | mc | How many random positions an insertion move may try before giving up. The run reports the failure and says which of mc_min_dist, mc_max_dist or this to change. | see `mc_min_dist`; see `mc_max_dist` |
| `mc_min_dist` | real | `0.2` | A | mc | Closest an inserted atom may come to an existing one. Trials nearer than this are rejected without evaluating the potential, which is what keeps an insertion from landing on top of an atom. | see `mc_max_dist` |
| `mc_move_max` | real | `1.0` | A | mc | Largest displacement of a single-atom "move". The trial displacement is drawn uniformly up to this. | needs `mc_types` |
| `mc_mu` | real list |  | eV | mc | Chemical potential of each species that may be inserted or removed, one value per entry in mc_species. Its length sets n_mc_mu. | sets `n_mc_mu`; see `mc_species`; see `mc_types` |
| `mc_mu_acceptance` | real list |  |  | mc | Relative probability of picking each species for a grand-canonical move, one weight per entry in mc_species. | sets `n_mc_types`; sets `n_mc_mu`; see `mc_mu`; see `mc_species` |
| `mc_n_planes` | integer | `0` |  | mc | Number of planes bounding the region moves are restricted to. Must appear before mc_planes and mc_max_dist_to_planes, which it allocates. | sets `mc_planes`; sets `mc_max_dist_to_planes`; see `mc_planes` |
| `mc_nrelax` | integer | `0` |  | mc | Number of relaxation steps to take after an accepted move when mc_relax is on. | needs `mc_relax` |
| `mc_nsteps` | integer | `1` |  | mc | Number of Monte-Carlo steps to take. |  |
| `mc_optimize_exp` | logical | `false` |  | mc | Include the experimental-data penalty in the Metropolis test, so the walk is driven towards agreement with the measured pattern. This is the reverse Monte-Carlo mode. | needs `do_exp`; see `exp_energy_scales` |
| `mc_planes` | real list |  |  | mc | The planes bounding the region moves are restricted to, four numbers per plane giving its normal and offset. mc_n_planes must appear first. | needs `mc_n_planes`; sets `mc_n_planes`; see `mc_max_dist_to_planes`; see `mc_planes_restrict_to_polyhedron` |
| `mc_planes_restrict_to_polyhedron` | logical | `false` |  | mc | Require an atom to be inside the polyhedron the planes enclose, rather than merely within mc_max_dist_to_planes of each plane. | needs `mc_planes` |
| `mc_relax` | logical | `false` |  | mc | Relax the geometry after each accepted move, so the walk samples relaxed configurations. | see `mc_nrelax`; see `mc_relax_opt`; see `mc_relax_after` |
| `mc_relax_after` | string list |  |  | mc | Which move types trigger a relaxation, by name. n_mc_relax_after must appear first. Without it, mc_relax applies to every accepted move. | needs `n_mc_relax_after`; sets `n_mc_relax_after`; see `mc_relax` |
| `mc_relax_opt` | string | `gd` |  | mc | How the post-move relaxation is driven: same choices as optimize. | needs `mc_relax`; see `optimize` |
| `mc_reverse` | logical |  |  | mc | **Accepted and ignored.** Reverse-Monte-Carlo switch from an earlier design; nothing consults it. Use mc_optimize_exp. | see `mc_optimize_exp` |
| `mc_reverse_lambda` | real |  |  | mc | **Accepted and ignored.** Mixing weight for the same earlier design; nothing consults it. The weight that does exist is exp_energy_scales. | see `exp_energy_scales` |
| `mc_species` | string list |  |  | mc | Species that may be inserted or removed by grand-canonical moves. Its length sets n_mc_mu. | sets `n_mc_mu`; see `mc_mu`; see `mc_types` |
| `mc_swaps` | string list |  |  | mc | Species pairs that a "swap" move may exchange, given as a flat list of symbols read two at a time. Its length sets n_mc_swaps, and the symbols are resolved against species. | sets `n_mc_swaps`; sets `species`; sets `mc_swaps_id`; see `mc_types`; see `species` |
| `mc_types` | string list |  |  | mc | Which moves the walk may make, from "move", "insertion", "removal", "relax", "md", "swap", "volume" and "none". Any other name aborts the run. Its length sets n_mc_types, and mc_acceptance weights them. | sets `n_mc_types`; see `mc_acceptance` |
| `mc_write_xyz` | logical | `false` |  | mc | Write the configuration after every Monte-Carlo step, not only the accepted ones. | see `write_xyz` |
| `n_mc_mu` | integer | `0` |  | mc | Number of species in the grand-canonical lists. Give it before mc_species, mc_mu and mc_mu_acceptance, which it allocates; giving those lists instead sets it implicitly. | sets `mc_mu`; sets `mc_species`; sets `mc_mu_acceptance`; see `mc_species` |
| `n_mc_relax_after` | integer | `0` |  | mc | Number of entries in mc_relax_after, which it allocates and must precede. | sets `mc_relax_after`; see `mc_relax_after` |
| `n_mc_swaps` | integer | `0` |  | mc | Number of swap pairs, which is half the length of mc_swaps. Give it before mc_swaps, which it allocates. | sets `mc_swaps`; sets `mc_swaps_id`; see `mc_swaps` |
| `n_mc_types` | integer | `0` |  | mc | Number of move types. Give it before mc_types and mc_acceptance, which it allocates; giving those lists instead sets it implicitly. | sets `mc_types`; sets `mc_acceptance`; see `mc_types` |

## Van der Waals

Tkatchenko-Scheffler and many-body dispersion.

| Keyword | Type | Default | Units | Modes | Description | Depends on |
| --- | --- | --- | --- | --- | --- | --- |
| `do_nnls` | logical | `false` |  | all | Solve the Hirshfeld-volume fit with non-negative least squares, which keeps the fitted volumes positive where an unconstrained solve would not. | needs `vdw_type` |
| `mbd_correction_freq` | integer | `100` |  | md, mc | Recompute the many-body dispersion every this many MD steps, reusing the previous correction in between. MBD is the expensive part of ts+mbd, so this trades accuracy for time. | needs `vdw_type` |
| `poly_cut_xmax` | real | `10.0` | A | all | Upper end of the range over which the polynomial cutoff turns the dispersion off. | needs `vdw_polynomial`; see `poly_cut_xmin` |
| `poly_cut_xmin` | real | `3.0` | A | all | Lower end of that range: below it the interaction is at full strength. | needs `vdw_polynomial`; see `poly_cut_xmax` |
| `print_estat_forces` | logical | `false` |  | all | Write the electrostatic contribution to the forces separately, for checking how much of the total it is. | see `estat_method` |
| `print_vdw_forces` | logical | `false` |  | all | Write the dispersion contribution to the forces separately. | see `vdw_type` |
| `vdw_2b_rcut` | real | `15.0` | A | all | Cutoff of the pairwise part of the many-body dispersion. | needs `vdw_type`; see `vdw_2b_rcut2` |
| `vdw_2b_rcut2` | real | `8.0` | A | all | Inner radius at which that pairwise part starts to be turned off; it is at full strength inside this and zero beyond vdw_2b_rcut. | needs `vdw_type`; see `vdw_2b_rcut` |
| `vdw_alpha0_ref` | real list |  | bohr^3 | all | Free-atom static polarizability of each species, one value per entry in species. Together with vdw_c6_ref and vdw_r0_ref these are the reference data the Hirshfeld rescaling starts from. | needs `vdw_type`; see `vdw_c6_ref`; see `vdw_r0_ref` |
| `vdw_buffer` | real | `1.0` | A | all | Width of the smooth turn-off applied at vdw_rcut, so the dispersion energy and its gradient reach zero together. | needs `vdw_type`; see `vdw_rcut` |
| `vdw_buffer_inner` | real | `0.5` | A | all | Width of the smooth turn-on applied at vdw_rcut_inner. | needs `vdw_type`; see `vdw_rcut_inner` |
| `vdw_c6_ref` | real list |  | Ha bohr^6 | all | Free-atom C6 coefficient of each species, one value per entry in species. | needs `vdw_type`; see `vdw_alpha0_ref`; see `vdw_r0_ref` |
| `vdw_d` | real | `20.0` |  | all | Steepness of the Fermi damping function in the Tkatchenko-Scheffler energy. Larger makes the switch at vdw_sr*R0 sharper. | needs `vdw_type`; see `vdw_sr` |
| `vdw_d_mbd` | real | `6.0` |  | all | The same steepness for the many-body dispersion. | needs `vdw_type`; see `vdw_sr_mbd` |
| `vdw_hirsh_grad` | logical | `true` |  | all | Include the derivative of the Hirshfeld volumes with respect to positions in the dispersion forces. Switching it off gives a cheaper but inconsistent force. | needs `vdw_type` |
| `vdw_loc_rcut` | real | `5.0` | A | all | Radius of the local environment whose atoms enter the screened polarizability of a given atom. | needs `vdw_type` |
| `vdw_mbd_cent_appr` | logical | `true` |  | all | Evaluate the many-body dispersion about the central atom only, rather than over every atom of its environment. The cheaper approximation. | needs `vdw_type` |
| `vdw_mbd_grad` | logical | `true` |  | all | Compute the gradient of the many-body dispersion energy. Off gives MBD energies with TS-only forces. | needs `vdw_type` |
| `vdw_mbd_nfreq` | integer | `13` |  | all | Number of imaginary-frequency quadrature points used for the dynamic polarizability. More is more accurate and proportionally slower. | needs `vdw_type` |
| `vdw_mbd_norder` | integer | `6` |  | all | Order at which the many-body expansion is truncated. | needs `vdw_type` |
| `vdw_mbd_rcut` | real | `15.0` | A | all | Cutoff of the many-body dispersion term. | needs `vdw_type`; see `vdw_mbd_rcut2` |
| `vdw_mbd_rcut2` | real | `8.0` | A | all | Inner radius at which the many-body term starts to be turned off. | needs `vdw_type`; see `vdw_mbd_rcut` |
| `vdw_omega_ref` | real | `1.3` | Ha | all | Reference characteristic excitation frequency of the oscillator model. | needs `vdw_type` |
| `vdw_polynomial` | logical | `false` |  | all | Use a polynomial cutoff for the dispersion rather than the default form. | needs `vdw_type`; see `poly_cut_xmin`; see `poly_cut_xmax` |
| `vdw_r0_ref` | real list |  | bohr | all | Free-atom van der Waals radius of each species, one value per entry in species. | needs `vdw_type`; see `vdw_c6_ref`; see `vdw_alpha0_ref` |
| `vdw_rcut` | real | `25.0` | A | all | Cutoff of the pairwise Tkatchenko-Scheffler dispersion. | needs `vdw_type`; see `vdw_buffer` |
| `vdw_rcut_inner` | real | `0.5` | A | all | Radius below which the dispersion is smoothly switched off, so that it does not double-count the short-range interaction the GAP already describes. | needs `vdw_type`; see `vdw_buffer_inner` |
| `vdw_scs_rcut` | real | `5.0` | A | all | Cutoff of the self-consistent screening that turns free-atom polarizabilities into screened ones. | needs `vdw_type` |
| `vdw_sr` | real | `0.94` |  | all | Position of the Fermi damping function in the Tkatchenko-Scheffler energy, as a fraction of the sum of the two van der Waals radii. Fitted per exchange-correlation functional. | needs `vdw_type`; see `vdw_d` |
| `vdw_sr_mbd` | real | `0.83` |  | all | The same fraction for the many-body dispersion. | needs `vdw_type`; see `vdw_d_mbd` |
| `vdw_type` | string | `none` |  | all | Which dispersion correction to add: "none", "ts" for Tkatchenko-Scheffler, "mbd" for many-body dispersion, or "ts+mbd" for both. Any other value aborts the run. The correction needs Hirshfeld volumes, so the GAP has to provide them. |  |

## Electrostatics

Charge-charge interactions and their damping.

| Keyword | Type | Default | Units | Modes | Description | Depends on |
| --- | --- | --- | --- | --- | --- | --- |
| `estat_damped` | logical |  |  | all | Damp the Coulomb interaction at the cutoff rather than truncating it, which removes the jump in the energy an atom crossing the cutoff would otherwise cause. | needs `estat_method` |
| `estat_damped_cosine` | logical |  |  | all | Use a cosine taper for that damping instead of the default shifted form. | needs `estat_method` |
| `estat_dsf_alpha` | real | `-1.0` | 1/A | all | Damping parameter of the damped shifted force. A negative value, the default, means the run picks one from the cutoff. | needs `estat_method` |
| `estat_gsf` | logical |  |  | all | Use the gradient-shifted force, which subtracts the force at the cutoff so that the force goes to zero there. | needs `estat_method` |
| `estat_inner_width` | real | `1.0` | A | all | Width over which the inner cutoff turns the electrostatics on. | needs `estat_method`; see `estat_rcut_inner` |
| `estat_method` | string | `none` |  | all | Which electrostatic sum to use: "none", "direct" for a bare cutoff sum, "dsf" for the damped shifted force, or "gsf" for the gradient-shifted force. An unknown name warns and computes nothing. The GAP has to provide charges for any of this to run. |  |
| `estat_rcut` | real | `10.0` | A | all | Cutoff of the electrostatic sum. | needs `estat_method` |
| `estat_rcut_inner` | real | `4.0` | A | all | Radius below which the electrostatics is smoothly switched off, so it does not double-count what the GAP already describes at short range. | needs `estat_method`; see `estat_inner_width` |
| `estat_self_energy_correction` | logical |  |  | all | Subtract each charge's interaction with its own periodic images, the self term of the shifted sum. | needs `estat_method` |
| `estat_sp` | logical |  |  | all | Use the shifted-potential form, which shifts the energy to zero at the cutoff but leaves a discontinuous force. | needs `estat_method` |
| `estat_tsf` | logical |  |  | all | Use the truncated shifted force. On by default. | needs `estat_method` |

## Experimental data (MAD)

XPS, pair distributions, structure factors and diffraction.

| Keyword | Type | Default | Units | Modes | Description | Depends on |
| --- | --- | --- | --- | --- | --- | --- |
| `do_exp` | logical | `false` |  | all | Compare the prediction against experimental data and add the mismatch to the energy. The individual exp_ keywords switch this on for you. | see `exp_data_files`; see `exp_energy_scales` |
| `do_nd` | logical | `false` |  | all | Compute a neutron diffraction pattern. Needs the partial pair distributions and the structure factors it is built from, so it switches both on. | sets `do_pair_distribution`; sets `do_structure_factor`; see `do_pair_distribution`; see `do_structure_factor`; see `nd_output` |
| `do_pair_distribution` | logical | `false` |  | all | Compute the pair distribution function g(r). Also records that the deck asked for it in as many words, which is what lets xrd_debye tell a request it must honour from a side effect it may undo. | sets `do_pair_distribution_explicit`; see `pair_distribution_rcut`; see `pair_distribution_n_samples` |
| `do_structure_factor` | logical | `false` |  | all | Compute the structure factor S(q). Needs the pair distributions when structure_factor_from_pdf is on, so it switches those on too. | sets `do_structure_factor_explicit`; sets `do_pair_distribution`; see `structure_factor_from_pdf`; see `q_range_min`; see `q_range_max` |
| `do_xps` | logical | `false` |  | all | Compute an X-ray photoelectron spectrum from the predicted core-electron binding energies. The GAP has to provide those. | see `xps_sigma`; see `xps_e_min`; see `xps_e_max` |
| `do_xrd` | logical | `false` |  | all | Compute an X-ray diffraction pattern. Needs the partial pair distributions and the structure factors it is built from, so it switches both on, unless xrd_debye takes the direct route instead. | sets `do_pair_distribution`; sets `do_structure_factor`; see `xrd_wavelength`; see `xrd_debye`; see `xrd_output` |
| `exp_data_files` | string list |  |  | all | Files holding the measured data, one per observable, in the order of exp_labels. Two columns: abscissa and intensity. | sets `exp_energy_scales`; see `exp_labels`; see `n_exp` |
| `exp_energies` | logical | `true` |  | all | Add the data mismatch to the energy. Naming it enables do_exp. | sets `do_exp`; see `exp_energy_scales` |
| `exp_energy_scales` | real list |  | eV | all | Weight of each observable's mismatch in the total energy, one value per observable. This is what sets how hard the data pulls against the potential. Also seeds the initial and final values of a ramp. | sets `n_exp`; sets `exp_energy_scales_initial`; sets `exp_energy_scales_final`; see `exp_energy_scales_final`; see `n_exp` |
| `exp_energy_scales_final`<br>`exp_energy_scales_end` | real list | `same as exp_energy_scales` | eV | all | End point of a linear ramp in the weights over the run, so the data can be brought in gradually. Without it the weights stay at exp_energy_scales. | see `exp_energy_scales` |
| `exp_forces` | logical | `false` |  | all | Compute the derivative of the data mismatch with respect to positions and add it to the forces. Naming it enables do_exp. Without it the mismatch contributes to the energy alone. | sets `do_exp`; see `exp_energies` |
| `exp_input_type` | string list |  |  | all | What the corresponding data file contains, one per observable, when it is not the observable's default representation: for instance "D(r)" for a pair distribution supplied as the reduced form. | sets `exp_energy_scales`; see `exp_labels`; see `pair_distribution_output` |
| `exp_labels` | string list |  |  | all | Which observable each data file is, one per file, from "xps", "xrd", "saxs", "pair_distribution" and "structure_factor". Matching a label to a file is what marks that observable valid, and an observable that was asked for but has no data stays switched off. | sets `exp_energy_scales`; sets `xps_idx`; sets `xrd_idx`; sets `valid_xrd`; sets `pair_distribution_partial`; sets `nd_idx`; sets `valid_nd`; sets `pdf_idx`; sets `valid_pdf`; sets `sf_idx`; sets `valid_sf`; see `exp_data_files`; see `n_exp` |
| `exp_n_samples` | integer list |  |  | all | Number of points to predict for each observable, one per observable, overriding that observable's own n_samples keyword. | sets `exp_energy_scales`; see `exp_labels` |
| `exp_similarity_type` | string | `squared_diff` |  | all | How the mismatch between prediction and data is measured: "squared_diff" for a sum of squared residuals, or "similarity"/"overlap" for a normalised overlap. | see `exp_energy_scales` |
| `n_exp` | integer | `0` |  | all | Number of experimental observables. Give it before exp_data_files, exp_labels and exp_energy_scales, which it allocates. | sets `exp_data_files`; sets `exp_energy_scales`; sets `exp_energy_scales_initial`; sets `exp_energy_scales_final`; sets `do_exp`; see `exp_labels` |
| `nd_n_samples` | integer | `200` |  | all | Number of points in the predicted neutron pattern. Writes the same sample count the XRD and structure-factor routines use, since the three share one q grid. | sets `xrd_n_samples`; sets `structure_factor_n_samples`; see `xrd_n_samples` |
| `nd_output` | string | `xrd` |  | all | Abscissa of the neutron pattern, in the same choices as xrd_output. Writes xrd_output as well, the two patterns sharing a grid. | sets `xrd_output`; see `xrd_output` |
| `nd_rcut` | real | `4.0` | A | all | Real-space cutoff of the pair sum entering the neutron pattern. | see `pair_distribution_rcut` |
| `nd_wavelength` | real |  |  | all | **Accepted and ignored.** Neutron wavelength. Nothing consults it: the pattern is built on the grid xrd_wavelength defines. | see `xrd_wavelength` |
| `pair_distribution_kde_sigma` | real | `0.0` | A | all | Width of the Gaussian each pair distance is smeared with when binning g(r). Zero, the default, means plain histogram binning. A non-zero width makes g(r) differentiable, which is what the forces need. | see `exp_forces` |
| `pair_distribution_n_samples` | integer | `200` |  | all | Number of r points in the predicted g(r). | see `r_range_min`; see `r_range_max` |
| `pair_distribution_output` | string | `pdf` |  | all | What g(r) is reported as: "pdf" for g(r) itself, or "D(r)" for the reduced pair distribution 4 pi r rho (g(r) - 1). The energy and its gradient follow the choice, so it changes the fit and not only the plot. | see `exp_input_type` |
| `pair_distribution_partial` | logical | `true` |  | all | Resolve g(r) into per-species-pair partials. The structure factor is built from these, so it is on by default. | see `do_structure_factor` |
| `pair_distribution_rcut` | real | `4.0` | A | all | Largest pair distance binned into g(r). A hard cut, not a smooth one, which is why a finite-difference check of the virial needs a small strain. | see `r_range_max` |
| `q_range_max` | real | `5.0` | 1/A | all | Upper end of the q range the structure factor and the diffraction patterns are predicted on. | see `q_range_min`; see `q_units` |
| `q_range_min` | real | `1.0` | 1/A | all | Lower end of that range. | see `q_range_max` |
| `q_units` | string | `q` |  | all | What the abscissa of the supplied and predicted patterns means: "q" or "saxs" for a scattering vector, "xrd" or "twotheta" for a diffraction angle. The conversion between them uses xrd_wavelength. | see `xrd_wavelength` |
| `r_range_max` | real | `5.0` | A | all | Upper end of the r range g(r) is reported on. Points beyond pair_distribution_rcut are empty. | see `pair_distribution_rcut` |
| `r_range_min` | real | `1.0` | A | all | Lower end of that range. | see `r_range_max` |
| `sf_output` | string | `xrd` |  | all | Abscissa of the structure factor, in the same choices as q_units. | see `q_units` |
| `structure_factor_from_pdf` | logical | `true` |  | all | Build S(q) by Fourier-transforming the binned partial pair distributions, rather than from the positions directly. The transform is far cheaper and is what the analytic forces are written for. | see `pair_distribution_partial` |
| `structure_factor_matrix` | logical | `true` |  | all | Assemble the transform as a matrix product over the r grid instead of looping the pairs. Much faster, at the cost of holding the kernel matrix. | see `structure_factor_matrix_forces` |
| `structure_factor_matrix_forces` | logical | `true` |  | all | Use the same matrix assembly for the derivatives. Falls back to the loop when the run cannot batch. | see `structure_factor_matrix` |
| `structure_factor_n_samples` | integer | `200` |  | all | Number of q points in the predicted S(q). | see `q_range_min`; see `q_range_max` |
| `structure_factor_window` | logical | `true` |  | all | Apply a Lorch window to the truncated pair distribution before transforming, which suppresses the ringing the hard cut at pair_distribution_rcut would otherwise put into S(q). | see `pair_distribution_rcut` |
| `xps_e_max` | real | `300.0` | eV | all | Upper end of the binding-energy range the XPS spectrum is predicted on. | needs `do_xps`; see `xps_e_min` |
| `xps_e_min` | real | `280.0` | eV | all | Lower end of that range. | needs `do_xps`; see `xps_e_max` |
| `xps_force_type` | string |  |  | all | **Accepted and ignored.** Which XPS force expression to use. Nothing consults it; the spectrum is differentiated one way only. |  |
| `xps_n_samples` | integer | `200` |  | all | Number of points in the predicted XPS spectrum. | needs `do_xps` |
| `xps_sigma` | real | `0.4` | eV | all | Width of the Gaussian each core-electron binding energy is broadened by to make a spectrum. | needs `do_xps` |
| `xrd_alpha` | real | `1.01` |  | all | Exponent of the sharpening applied to the diffraction pattern. | needs `do_xrd`; see `xrd_damping` |
| `xrd_damping` | real | `0.0` |  | all | Damping applied to the pair sum with distance, which suppresses the truncation ripple from the finite cutoff. Zero, the default, is no damping. | needs `do_xrd`; see `xrd_alpha` |
| `xrd_debye` | logical | `false` |  | all | Compute the pattern from the N^2 Debye sum over positions instead of transforming the partial pair distributions. The two routes answer the same question by different approximations: the pdf route bins distances and truncates at pair_distribution_rcut, the Debye route sums every pair exactly. Turning it on makes the pdf and sf calculations unnecessary, and whichever of them was switched on only to feed the XRD is switched off again. | needs `do_xrd`; see `pair_distribution_rcut` |
| `xrd_iwasa` | logical | `true` |  | all | **Deprecated.** Iwasa correction switch from an earlier design; the correction is applied unconditionally now. |  |
| `xrd_lorentz_polarization` | logical | `false` |  | all | Multiply the predicted pattern by the powder Lorentz-polarization factor. Only the Debye route applies it, where the energy and the gradient stay consistent because it is one multiplicative weight per q. | needs `xrd_debye`; see `xrd_lp_polarization` |
| `xrd_lp_polarization` | real | `1.0` |  | all | Degree of polarization P of the incident beam in the factor (1 + P cos^2 2theta) / (sin^2 theta cos theta). P = 1 is an unpolarized source; a graphite monochromator at 2theta_M gives P = cos^2(2theta_M). | needs `xrd_lorentz_polarization` |
| `xrd_lp_sin_theta_min` | real | `1.e-3` |  | all | Below this \|sin theta\| the Lorentz factor 1/(sin^2 theta cos theta) is unusable, so the whole factor is set to zero rather than blown up. | needs `xrd_lorentz_polarization` |
| `xrd_method` | string | `xrd` |  | all | Which scattering factors the pattern is built from. "xrd" uses the X-ray form factors. | needs `do_xrd` |
| `xrd_n_samples` | integer | `200` |  | all | Number of points in the predicted X-ray pattern. Sets the structure-factor sample count too, the two sharing a q grid. | needs `do_xrd`; sets `structure_factor_n_samples` |
| `xrd_output` | string | `xrd` |  | all | Abscissa the pattern is reported on, in the same choices as q_units. | needs `do_xrd`; see `q_units` |
| `xrd_rcut` | real | `4.0` | A | all | Real-space cutoff of the pair sum entering the X-ray pattern. | needs `do_xrd`; see `pair_distribution_rcut` |
| `xrd_wavelength` | real | `1.5405981` | A | all | Wavelength of the incident X-rays. Fixes the map between q and 2theta, so it matters whenever q_units or xrd_output name an angle. | see `q_units` |

## Electronic stopping and EPH

Radiation cascades and electron-phonon coupling.

| Keyword | Type | Default | Units | Modes | Description | Depends on |
| --- | --- | --- | --- | --- | --- | --- |
| `adapt_emax` | real | `1.0e1` | eV | md | Largest energy change tolerated over one step; the adaptive time step shrinks until the change is under this. | needs `adaptive_time` |
| `adapt_tmax` | real | `1.0` | fs | md | Upper bound on the adaptive time step. | needs `adaptive_time` |
| `adapt_tmin` | real | `1.0e-3` | fs | md | Lower bound on the adaptive time step. Reaching it means the dynamics cannot be resolved and the run should be reconsidered. | needs `adaptive_time` |
| `adapt_tstep_interval` | integer | `1` |  | md | How often, in steps, the adaptive time step is re-examined. | needs `adaptive_time` |
| `adapt_xmax` | real | `1.0e-2` | A | md | Largest displacement of any atom tolerated over one step. | needs `adaptive_time` |
| `adaptive_time` | logical | `false` |  | md | Choose the time step from how fast the fastest atom is moving, rather than holding md_step fixed. Written for radiation cascades, where the first femtoseconds need a far smaller step than the rest. | see `adapt_tmin`; see `adapt_tmax`; see `adapt_xmax`; see `adapt_emax` |
| `eel_cut` | real | `1.0` | eV | md | Kinetic energy below which an atom is too slow to lose energy to the electrons, so electronic stopping is not applied to it. | needs `electronic_stopping` |
| `eel_freq_out` | integer | `1` |  | md | How often, in steps, to report the energy lost to electronic stopping. | needs `electronic_stopping` |
| `electronic_stopping` | logical | `false` |  | md | Drain energy from fast atoms into the electronic system, the dominant energy loss at the start of a radiation cascade. Reads its stopping powers from estop_filename. | see `estop_filename`; see `eel_cut` |
| `eph_betafile` | string | `NULL` |  | md | File holding the beta(rho) coupling data the EPH model needs, one entry per species. | needs `nonadiabatic_processes` |
| `eph_box_limits` | real(6) | `[-100.0, 100.0, -100.0, 100.0, ...` | A | md | Extent of the electronic grid, as xmin xmax ymin ymax zmin zmax. Also written into the six individual bounds the solver reads. | needs `nonadiabatic_processes`; sets `in_x0`; sets `in_x1`; sets `in_y0`; sets `in_y1`; sets `in_z0`; sets `in_z1` |
| `eph_c_e` | real | `1.0` | eV/A^3/K | md | Electronic heat capacity per unit volume, used when the electronic temperature is evolved rather than held fixed. | needs `nonadiabatic_processes` |
| `eph_e_prev_time` | real | `0.0` | eV | md | Electronic energy carried over from a previous run, for restarting a cascade. | needs `nonadiabatic_processes` |
| `eph_fdm_option` | integer | `1` |  | md | How the electronic temperature grid is initialised: from eph_Tinfile, or uniformly at eph_Ti_e. | needs `nonadiabatic_processes` |
| `eph_fdm_steps` | integer | `1` |  | md | Number of electronic diffusion sub-steps taken per MD step, the electronic system being much faster than the ionic one. | needs `nonadiabatic_processes` |
| `eph_freq_mesh_tout` | integer | `1` |  | md | How often, in steps, to write the whole electronic temperature grid. | needs `nonadiabatic_processes` |
| `eph_freq_tout` | integer | `1` |  | md | How often, in steps, to write the electronic temperature summary. | needs `nonadiabatic_processes` |
| `eph_friction_option` | integer | `1` |  | md | Which friction term of the electron-phonon coupling to apply, or none. | needs `nonadiabatic_processes` |
| `eph_gsx` | integer | `1` |  | md | Number of electronic grid cells along x. | needs `nonadiabatic_processes` |
| `eph_gsy` | integer | `1` |  | md | Number of electronic grid cells along y. | needs `nonadiabatic_processes` |
| `eph_gsz` | integer | `1` |  | md | Number of electronic grid cells along z. | needs `nonadiabatic_processes` |
| `eph_kappa_e` | real | `1.0` | eV/A/fs/K | md | Electronic thermal conductivity entering the diffusion of the electronic temperature. | needs `nonadiabatic_processes` |
| `eph_md_last_step` | integer | `0` |  | md | Step number the previous run finished at, for restarting a cascade. | needs `nonadiabatic_processes` |
| `eph_md_prev_time` | real | `0.0` | fs | md | Simulated time the previous run finished at, for restarting a cascade. | needs `nonadiabatic_processes` |
| `eph_random_option` | integer | `1` |  | md | Which random term of the electron-phonon coupling to apply, or none. Paired with the friction term it is what makes the coupling a thermostat rather than a drain. | needs `nonadiabatic_processes`; see `eph_friction_option` |
| `eph_rho_e` | real | `1.0` | 1/A^3 | md | Electronic density entering the coupling. | needs `nonadiabatic_processes` |
| `eph_ti_e` | real | `300.0` | K | md | Initial electronic temperature, when the grid is started uniform rather than read from a file. | needs `nonadiabatic_processes`; see `eph_fdm_option` |
| `eph_tinfile` | string | `NULL` |  | md | File holding an initial electronic temperature grid. | needs `nonadiabatic_processes` |
| `eph_toutfile` | string | `NULL` |  | md | File the electronic temperature grid is written to. | needs `nonadiabatic_processes` |
| `estop_filename` | string | `NULL` |  | md | File holding the electronic stopping power per species as a function of velocity. | needs `electronic_stopping` |
| `model_eph` | integer | `1` |  | md | Which electron-phonon model to evaluate. | needs `nonadiabatic_processes` |
| `nonadiabatic_processes` | logical | `false` |  | md | Exchange energy between the ions and an explicit electronic subsystem through the EPH model, rather than the one-way drain of electronic stopping. | see `model_eph`; see `eph_friction_option`; see `eph_random_option` |

## Local properties

Per-atom quantities predicted alongside the energy.

| Keyword | Type | Default | Units | Modes | Description | Depends on |
| --- | --- | --- | --- | --- | --- | --- |
| `compute_local_properties` | string list |  |  | all | Which local properties to predict, by name, one per property. The GAP has to carry a model for each. n_local_properties must appear first. | needs `n_local_properties` |
| `core_pot_buffer` | real | `1.0` | A | all | Width over which the core potential is smoothly turned off approaching core_pot_cutoff, so the tabulated potential and its gradient reach zero together. | see `core_pot_cutoff` |
| `core_pot_cutoff` | real | `1.e10` | A | all | Distance beyond which the tabulated core potential is dropped. The default is effectively infinite, meaning the whole table is used. | see `core_pot_buffer` |
| `n_local_properties` | integer | `0` |  | all | Number of local properties to predict. Give it before compute_local_properties and write_local_properties, which it allocates. | sets `write_local_properties`; sets `compute_local_properties`; see `compute_local_properties` |
| `print_lp_forces` | logical |  |  | all | **Accepted and ignored.** Local-property force reporting switch; nothing consults it. |  |

## Output

What is written, and how often.

| Keyword | Type | Default | Units | Modes | Description | Depends on |
| --- | --- | --- | --- | --- | --- | --- |
| `print_progress` | logical | `true` |  | all | Print a progress line as the run advances. |  |
| `write_exp` | logical | `true` |  | all | Write the predicted experimental observables to file. | needs `do_exp` |
| `write_fixes` | logical | `true` |  | all | Include the per-atom fix flags as a column in the XYZ output. |  |
| `write_forces` | logical | `true` |  | all | Include forces as columns in the XYZ output. |  |
| `write_hirshfeld_v` | logical |  |  | all | **Accepted and ignored.** Hirshfeld-volume output switch; the volumes follow the local-property output instead, so nothing consults it. | see `write_local_properties` |
| `write_local_energies` | logical | `true` |  | all | Include the per-atom energy decomposition as a column in the XYZ output. |  |
| `write_local_properties` | logical list |  |  | all | Which predicted local properties to write, one flag per property in the order of compute_local_properties. | needs `n_local_properties`; see `compute_local_properties` |
| `write_lv` | logical | `false` |  | all | Write the lattice vectors on every frame. |  |
| `write_masses` | logical | `false` |  | all | Include atomic masses as a column in the XYZ output. |  |
| `write_nd` | logical | `false` |  | all | Write the predicted neutron diffraction pattern to file. | needs `do_nd` |
| `write_pair_distribution` | logical | `false` |  | all | Write the predicted pair distribution to file. | needs `do_pair_distribution` |
| `write_pressure` | logical | `true` |  | all | Include the pressure in the frame comment line. |  |
| `write_stress` | logical | `true` |  | all | Include the stress tensor in the frame comment line. | see `write_virial` |
| `write_structure_factor` | logical | `false` |  | all | Write the predicted structure factor to file. | needs `do_structure_factor` |
| `write_thermo` | integer | `1` |  | md, mc | Write a thermodynamic summary line every this many steps. Zero switches it off. |  |
| `write_velocities` | logical | `true` |  | all | Include velocities as columns in the XYZ output. |  |
| `write_virial` | logical | `true` |  | all | Include the virial tensor in the frame comment line. | see `write_stress` |
| `write_xrd` | logical | `false` |  | all | Write the predicted X-ray diffraction pattern to file. | needs `do_xrd` |
| `write_xyz` | integer | `0` |  | md, mc | Write a trajectory frame every this many steps. Zero, the default, writes only the last. |  |

## GPU

Accepted by the CPU build too, so one deck runs on both.

| Keyword | Type | Default | Units | Modes | Description | Depends on |
| --- | --- | --- | --- | --- | --- | --- |
| `gpu_batched` | logical | `true` |  | all | Evaluate the experimental observables in batches on the device, staging one batch of pairs at a time. Switching it off takes the unbatched route, which holds everything at once and is the reference the batched route is checked against. |  |
| `gpu_low_memory` | logical | `true` |  | all | Free each batch's device arrays as soon as its contribution has been accumulated, rather than holding them for the whole calculation. Slower and much smaller. | see `gpu_batched` |
| `gpu_max_batch_size` | real | `1.0` | GB | all | Largest device allocation a single batch may make. The batch count follows from it. | see `gpu_n_batches` |
| `gpu_n_batches` | integer | `1` |  | all | Split the work into exactly this many batches, instead of choosing the count from the memory budget. | see `gpu_max_batch_size` |
| `n_batches` | integer | `0` |  | all | Split the SOAP descriptor calculation into exactly this many batches, instead of choosing the count from max_Gbytes_per_process. | see `max_gbytes_per_process` |

# Keywords for the potential (.gap) file

The fitted potential, named by `pot_file` in the input file. It is a
sequence of blocks, each opened by `gap_beg <descriptor>` and closed by
`gap_end`, holding one `keyword = value` per line. `gap_beg` and `gap_end`
are file syntax rather than settings and are not listed below. Unlike the
input file, a keyword the block does not recognise is skipped in silence,
so a misspelling here shows up as a wrong answer rather than an error.
These files are written by the fitting code, not by hand.

## soap_turbo

The SOAP descriptor and the GAP fitted on it, one block per central species. Also carries the local-property models -- Hirshfeld volumes, charges, core-electron binding energies -- that ride on the same descriptor.

| Keyword | Type | Default | Units | Description | Depends on |
| --- | --- | --- | --- | --- | --- |
| `alphas_sparse` | string |  |  | File holding the fitted sparse-set coefficients, one per sparse point. Read together with desc_sparse, which must have the same number of rows. | see `desc_sparse` |
| `amplitude_scaling` | real list |  |  | How the amplitude of a neighbour's Gaussian falls with distance, one per species, in the form set by scaling_mode. Larger values weight the near neighbours more heavily. | see `scaling_mode` |
| `atom_sigma_r` | real list |  | A | Radial width of the Gaussian each neighbour is smeared into, one per species. Small values resolve fine radial structure and need a larger n_max to represent. | see `atom_sigma_r_scaling`; see `n_max` |
| `atom_sigma_r_scaling` | real list |  |  | How the radial width grows with neighbour distance, one per species: the width used is atom_sigma_r * (1 + scaling * r). Letting distant neighbours blur is what keeps the descriptor from resolving structure the fit cannot support. | see `atom_sigma_r` |
| `atom_sigma_t` | real list |  | A | Angular width of that Gaussian, measured as an arc length rather than an angle, one per species. Divided by the neighbour distance to give the angular spread, so a fixed value keeps the angular resolution roughly constant with distance. | see `atom_sigma_t_scaling`; see `l_max` |
| `atom_sigma_t_scaling` | real list |  |  | The same linear growth for the angular width. | see `atom_sigma_t` |
| `basis` | string | `poly3` |  | Radial basis to expand the density in. "poly3" is the polynomial basis, and "poly3gauss" adds a Gaussian for the central atom. | see `n_max` |
| `buffer` | real list |  | A | Width of the buffer zone inside the hard cutoff over which a neighbour's contribution is smoothly taken to zero, one per species. Read as a width and converted to the soft cutoff rcut - buffer once the block closes; zero means no buffer, and the density is cut abruptly. | see `rcut` |
| `central_species` | integer | `0` |  | Which entry of species this descriptor is centred on, as a 1-based index. One soap_turbo block per central species is the usual arrangement, each seeing all of them as neighbours. | see `species` |
| `central_weight` | real list |  |  | Weight given to the central atom's own contribution to the density, one per species. Zero leaves the centre out of its own descriptor. |  |
| `compress_mode` | string | `none` |  | Named compression scheme to use, resolved into the projection internally. Use this or file_compress_soap, not both. | needs `compress_soap`; see `file_compress_soap` |
| `compress_soap` | logical | `false` |  | Project the descriptor onto a smaller set of components before the kernel is evaluated. The SOAP vector grows as n_max^2 l_max, and compression is what keeps a many-species descriptor affordable. | see `compress_mode`; see `file_compress_soap` |
| `delta` | real | `1.0` | eV | Energy scale of this GAP: the standard deviation of the prior on the energy it contributes. It sets how much of the total energy this descriptor is allowed to account for, and setting it to zero switches the descriptor's contribution off without removing the block. | see `zeta` |
| `desc_sparse` | string |  |  | File holding the sparse-set descriptors, one row per sparse point. Its row count sets n_sparse and so the cost of every prediction. | see `alphas_sparse` |
| `dipole_model` | logical | `false` |  | This descriptor's fitted scalar is not an energy but a potential whose gradient with respect to the central atom's own position is the local dipole. Its energy, forces and virial are meaningless and are not summed into the totals; only the dipole is taken. | see `delta` |
| `file_compress_soap` | string | `none` |  | File giving the compression explicitly, as the list of components to keep or a transformation matrix. The alternative to naming a compress_mode. | needs `compress_soap`; see `compress_mode` |
| `global_scaling` | real list |  |  | Overall multiplier on each species' contribution to the density, one per species. This is where a species is made to count for more or less than its neighbours. |  |
| `has_local_properties` | logical | `false` |  | This descriptor carries per-atom quantities fitted alongside the energy -- Hirshfeld volumes, charges, core-electron binding energies. Set implicitly by n_local_properties. | see `n_local_properties`; see `local_property_labels` |
| `has_vdw` | logical | `false` |  | **Deprecated.** Superseded by has_local_properties. A Hirshfeld-volume model is now one local property among several rather than a special case, and this keyword still sets up that one property so that older potential files load. | sets `has_local_properties`; sets `n_local_properties`; sets `vdw_index`; see `has_local_properties` |
| `l_max` | integer |  |  | Highest spherical-harmonic degree kept in the angular expansion. The descriptor's angular resolution, and the counterpart of n_max: the cost of the descriptor grows with both. | see `n_max`; see `atom_sigma_t` |
| `local_property_alphas` | type(local_property_soap_turbo) list |  |  | Sparse-set coefficient file for each local property, one per property. | needs `n_local_properties`; sets `n_local_properties`; see `local_property_qs` |
| `local_property_deltas` | type(local_property_soap_turbo) list |  |  | Prior scale for each local property, one per property, in whatever units that property has. | needs `n_local_properties`; see `delta` |
| `local_property_labels` | type(local_property_soap_turbo) list |  |  | What each local property is, one name per property. The names are meaningful, not decorative: "hirshfeld_v" is what the van der Waals correction looks for, and "core_electron_be" is what the XPS spectrum is built from. Naming either one switches on the machinery that consumes it and records which slot it occupies. | needs `n_local_properties`; sets `n_local_properties`; sets `has_vdw`; sets `vdw_index`; sets `has_core_electron_be`; sets `core_electron_be_index`; see `local_property_qs`; see `local_property_alphas` |
| `local_property_qs` | type(local_property_soap_turbo) list |  |  | Sparse-set descriptor file for each local property, one per property. | needs `n_local_properties`; sets `n_local_properties`; see `local_property_alphas` |
| `local_property_v0s` | type(local_property_soap_turbo) list |  |  | Baseline added to each local property's prediction, one per property. For Hirshfeld volumes this is the free-atom volume the fit is a correction to. | needs `n_local_properties` |
| `local_property_zetas` | integer | `0` |  | Kernel exponent for each local property, one per property. The local properties are fitted with their own kernels and need not share the energy's zeta. | needs `n_local_properties`; see `zeta` |
| `n_local_properties` | integer | `0` |  | How many local properties this descriptor carries. Give it before the local_property_* lists, which it allocates and which are all read this many values wide. | see `local_property_labels` |
| `n_max` | integer |  |  | Number of radial basis functions per species. The descriptor's radial resolution: it has to be large enough to represent features of width atom_sigma_r out to the cutoff. The block's total radial basis size is the sum over species. | sets `alpha_max`; see `l_max`; see `atom_sigma_r` |
| `n_species` | integer |  |  | Number of species appearing in the neighbour environments this descriptor sees. It must come first in the block: every per-species list below is read n_species values wide, and the block is pre-scanned for this keyword alone before anything else is parsed so that those arrays can be allocated. | sets `nf`; sets `rcut`; sets `buffer`; sets `atom_sigma_r`; sets `atom_sigma_t`; sets `atom_sigma_r_scaling`; sets `atom_sigma_t_scaling`; sets `amplitude_scaling`; sets `central_weight`; sets `global_scaling`; sets `n_max`; sets `species`; see `species` |
| `nf` | real list |  |  | Steepness of the radial filter applied to each species' density, one per species. Larger values make the transition sharper. | see `rcut`; see `buffer` |
| `radial_enhancement` | integer | `0` |  | Multiply the radial density by r raised to this power before expanding it, which weights the outer shells more heavily. 0, 1 and 2 are the usual choices. |  |
| `rcut` | real list |  | A | Hard cutoff, one per species: beyond it a neighbour contributes nothing. The largest over every descriptor in the file becomes the cutoff the neighbour lists are built to. | sets `rcut_max`; see `buffer` |
| `scaling_mode` | string | `polynomial` |  | Functional form amplitude_scaling is applied through. "polynomial" is the default and the only form the GPU kernels implement. | see `amplitude_scaling` |
| `species` | string list |  |  | Chemical symbols of the neighbour species, in the order every per-species list in this block is written. n_species entries. | see `n_species`; see `central_species` |
| `vdw_alphas` | string |  |  | **Deprecated.** Superseded by local_property_alphas. | see `local_property_alphas` |
| `vdw_delta` | real |  |  | **Deprecated.** Superseded by local_property_deltas. | see `local_property_deltas` |
| `vdw_qs` | string |  |  | **Deprecated.** Superseded by local_property_qs. | see `local_property_qs` |
| `vdw_v0` | real |  |  | **Deprecated.** Superseded by local_property_v0s. | see `local_property_v0s` |
| `vdw_zeta` | real |  |  | **Deprecated.** Superseded by local_property_zetas. | see `local_property_zetas` |
| `zeta` | real | `2.0` |  | Exponent of the SOAP kernel, (q . q')^zeta. Raising it sharpens the kernel and makes the fit more local in descriptor space. | see `delta` |

## distance_2b

A two-body GAP on the interatomic distance, one block per species pair.

| Keyword | Type | Default | Units | Description | Depends on |
| --- | --- | --- | --- | --- | --- |
| `Z1`<br>`z1`, `species1` | string |  |  | First species of the pair this block describes, as a chemical symbol. With Z2 it decides which pairs the descriptor is evaluated on. | see `Z2` |
| `Z2`<br>`z2`, `species2` | string |  |  | Second species of the pair. A block with Z1 and Z2 swapped would be the same interaction, so only one of the two orders is given. | see `Z1` |
| `alphas_sparse` | string |  |  | File holding the fitted sparse-set coefficients, one per sparse point. | see `desc_sparse` |
| `delta` | real | `1.0` | eV | Energy scale of this two-body GAP, the standard deviation of the prior on the energy it contributes. Zero switches the block off without removing it. | see `sigma` |
| `desc_sparse` | string |  |  | File holding the sparse-set descriptors, one distance per row. Its row count sets the number of sparse points. | see `alphas_sparse` |
| `rcut` | real |  | A | Cutoff of the pair interaction: beyond it the pair contributes nothing. |  |
| `sigma` | real | `1.0` | A | Width of the kernel in the descriptor, which here is the interatomic distance itself. It sets how far apart two distances have to be before the fit treats them as different. | see `delta` |

## angle_3b

A three-body GAP on the two distances and the angle between them, one block per centre-and-pair combination.

| Keyword | Type | Default | Units | Description | Depends on |
| --- | --- | --- | --- | --- | --- |
| `Z1`<br>`z1`, `species1` | string |  |  | Species of the first neighbour in the triplet. | see `Z_center`; see `Z2` |
| `Z2`<br>`z2`, `species2` | string |  |  | Species of the second neighbour. Swapping Z1 and Z2 gives the same triplet, so only one order is given and the descriptor is symmetrised internally. | see `Z1` |
| `Z_center`<br>`z_center`, `species_center` | string |  |  | Species at the vertex of the triplet, as a chemical symbol. The three-body term is centred on this atom with Z1 and Z2 as its two neighbours. | see `Z1`; see `Z2` |
| `alphas_sparse` | string |  |  | File holding the fitted sparse-set coefficients, one per sparse point. | see `desc_sparse` |
| `delta` | real | `1.0` | eV | Energy scale of this three-body GAP. Zero switches the block off without removing it. | see `sigma` |
| `desc_sparse` | string |  |  | File holding the sparse-set descriptors, one triplet per row, three components each. | see `alphas_sparse` |
| `kernel_type` | string | `exp` |  | Form of the three-body kernel. "exp" is the squared-exponential the fits use. | see `sigma` |
| `rcut` | real |  | A | Cutoff on each of the two distances from the centre. Both neighbours must be inside it for the triplet to contribute. |  |
| `sigma` | real(1:3) | `1.0` |  | Kernel widths of the three-body descriptor, three values: one for each of the two distances and one for the coordinate built from the angle between them. | see `delta`; see `kernel_type` |

## core_pot

A tabulated pair potential added on top of the GAPs, one block per species pair. Splined on read.

| Keyword | Type | Default | Units | Description | Depends on |
| --- | --- | --- | --- | --- | --- |
| `Z1`<br>`z1`, `species1` | string |  |  | First species of the pair this core potential acts on, as a chemical symbol. | see `Z2` |
| `Z2`<br>`z2`, `species2` | string |  |  | Second species of the pair. | see `Z1` |
| `core_pot_file` | string |  |  | File holding the tabulated potential, as distance and energy columns. It is splined on read, and truncated at core_pot_cutoff with a taper of width core_pot_buffer, both set in the input file rather than here. This is the short-range repulsion the GAP is not fitted to describe, so the table usually only covers distances the training data never visited. | see `core_pot_cutoff`; see `core_pot_buffer` |

